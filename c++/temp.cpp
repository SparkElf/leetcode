/**
 * map.count()
 *for (auto it = children.begin();it != children.end();it++)
        if (visited[*it])
            children.erase(it--);//必须要重置it
        else makeTree(*it);
printf注意 输出int %d long %ld longlong %lld
substr第二个参数是长度 若pos的值超过了string的大小，则substr函数会抛出一个out_of_range异常；若pos+n的值超过了string的大小，则substr会调整n的值，只拷贝到string的末尾
abs(-1)//https://www.jianshu.com/p/28d8899090f6
priority_queue<Node, vector<Node>, greater<Node>>q;
 **/
int main() {
    return 0;
}